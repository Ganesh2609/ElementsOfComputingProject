class Mod {

function int mod(int a, int b) {
        var int qoutient;
        var int reminder;
        let qoutient = (a / b);
        let reminder = a - (b * qoutient);
        return reminder;
    }
}